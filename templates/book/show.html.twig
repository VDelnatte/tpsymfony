{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
    <h1>{{ book.title }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ book.title }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ book.genre }}</td>
            </tr>
            <tr>
                <th>PublishedAt</th>
                <td>{{ book.publishedAt ? book.publishedAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    <a href="{{ path('app_book_edit', {'id': book.id}) }}">Modifier </a>
    {{ include('book/_delete_form.html.twig') }}
    <br>
    <a href="{{ path('app_book_index') }}">Retour à la liste </a><br>

{% endblock %}

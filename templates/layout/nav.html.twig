{% if app.user %}
{% block nav %}
<nav>
    <a href="{{ path('app_logout') }}">Déconnexion</a>
    <br>
    <br>
    <a href="{{ path('app_book_index')}}">Livres</a>
    <a href="{{ path('app_author_index')}}">Auteurs</a>
    <a href="{{ path('app_borrow_index')}}">Emprunts</a>
</nav>
{% endblock %}
{% endif %}